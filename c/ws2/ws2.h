

#ifndef __WS2_H__
#define __WS2_H__


	
	void swapValues(int*, int*);
	int* copyArray (int *ptr, int array_size);
	void printAdresses();
	void SwapsTypeT(size_t *num1, size_t *num2);
	void SwapsTypeT(size_t *num1, size_t *num2);
	void SwapsTypeTPointers(size_t **num1, size_t **num2);
	int IsPalindrome(char *str); /*return 0 if TRUE 1 if FALSE*/

#endif /*__WS2_H__*/
